┌─────────────────────────────────────────────────┐
│          LOCAL VOICE SYSTEM                     │
└─────────────────────────────────────────────────┘

voice_robot.py (Python)
    ↓
Mikrofon record audio (sounddevice)
    ↓
Whisper transcribe
    ↓
Process command
    ↓
Send to Arduino via Serial
    ↓
robot.ino (Arduino)
    ↓
LED/Motor action